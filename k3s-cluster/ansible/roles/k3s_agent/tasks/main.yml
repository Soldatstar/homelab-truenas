- name: Install K3s agent
  shell: |
    curl -sfL https://get.k3s.io | \
      K3S_URL=https://{{ hostvars['k3s-server-01'].ansible_host }}:6443 \
      K3S_TOKEN={{ hostvars['k3s-server-01'].k3s_token }} \
      sh -
  args:
    creates: /etc/rancher/k3s/k3s.yaml
    